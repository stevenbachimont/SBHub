version: '3.9'
services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "4173:4173"
    volumes:
      - ./static/downloads:/app/static/downloads
    environment:
      - NODE_ENV=production
